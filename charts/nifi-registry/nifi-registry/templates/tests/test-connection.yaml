---
# Source: nifi-registry/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "RELEASE-NAME-nifi-registry-test-connection"
  labels:
    helm.sh/chart: nifi-registry-0.1.1
    app.kubernetes.io/name: nifi-registry
    app.kubernetes.io/instance: RELEASE-NAME
    app.kubernetes.io/version: "0.6.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['RELEASE-NAME-nifi-registry:80']
  restartPolicy: Never
